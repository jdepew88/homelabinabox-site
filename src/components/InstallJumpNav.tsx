import { INSTALL_SECTIONS } from '../content/install-sections'
import { SectionJumpNav } from './SectionJumpNav'

export function InstallJumpNav() {
  return <SectionJumpNav sections={INSTALL_SECTIONS} />
}
